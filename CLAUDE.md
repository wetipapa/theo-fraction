# 분수 쓱싹 (WTPP SLICE)

튀어 오르는 음식 중에서 문제에 맞는 분수만 골라 베는 6~9세용 분수 게임.
상위 규칙은 `WTPP/CLAUDE.md`를 따르고, 여기에는 이 게임에만 해당하는 것만 적는다.

- 스택: Vite + React 19 + Tailwind v4 + Canvas 2D
- 분모는 부모가 고른다(2~9 복수 선택). 난이도는 속도와 폭탄만 맡는다
- 배포 예정 주소: `slice.wetipapa.com`
- 저장소: `wetipapa/theo-fraction`

## 이 게임에서 무엇을 배우는가

**"칠해진 칸이 전체의 얼마인가"를 눈으로 가늠하는 일.**

그래서 날아다니는 그림에 숫자를 적지 않는다. 숫자가 있으면 아이가 그림을 세지 않고
글자만 읽는다. 분수를 읽는 연습은 나중에 `read` 모드로 따로 만든다.

같은 1/2이라도 피자로 보면 부채꼴이고 초콜릿으로 보면 가로 칸이다.
한 모양만 쓰면 "반원 = 1/2"으로 외워 버려서 분수가 아니라 그림을 외우게 된다.
그래서 원형과 막대형을 섞는다.

## 분모는 부모가 고른다

난이도(쉬움·보통·어려움)는 **속도와 폭탄만** 맡는다. 분모는 따로 고른다.

아이마다 막히는 곳이 다르다. 2·3·4는 되는데 7·8만 안 되는 식이라,
난이도에 묶으면 "7등분을 연습하려면 어려움을 골라야 하는데 그러면 너무 빨라진다"가 된다.
형제 서비스(구구단 팡팡·구구단 레이싱)가 단을 따로 고르게 한 것과 같은 이유다.

## 난이도는 높이를 건드리지 않는다

**어느 난이도에서든 같은 높이로 뜬다** (화면 위에서 19% 지점).

예전에는 난이도마다 발사 배수를 곱했다. 그랬더니 쉬움이 화면 45% 지점까지만 떠서
아래쪽 좁은 구간을 스쳐 지나갔고, 정작 쉬움이 제일 어려웠다.
어려움은 반대로 화면 밖까지 날아가 그동안 보이지도 않았다.

난이도는 **개수(`maxConcurrent`)·간격(`interval`)·오답 수·폭탄·기회 수**로 준다. 빠르기는 따로 고른다.

## 설정은 세 축이다

| 축 | 무엇을 정하나 | 왜 따로 두나 |
|---|---|---|
| 분모 (2~9) | 어떤 분수가 나오나 | 아이마다 막히는 곳이 다르다 |
| 떨어지는 빠르기 (1~5) | 생각할 시간 | 조각 세는 속도는 아이마다 다르다 |
| 난이도 (쉬움·보통·어려움) | 동시 개수·오답 수·폭탄·기회 | 문제의 어려움 |

셋을 하나로 묶으면 "천천히 하려면 문제도 쉬워져야" 한다.
오답을 많이 섞되 천천히 떨어지길 바랄 수도 있고, 그 반대일 수도 있다.

**빠르기가 5단계인 이유:** 10칸으로 쪼개면 한 칸이 8%씩이라 인접한 값을 구분해 고를 수 없다.
5칸이면 한 칸이 15~20%라 눌러 보면 바로 체감된다.

**속도만큼이나 동시 개수가 시간을 잡아먹는다.** 셋이 떠 있으면 체공을 아무리 늘려도
하나당 볼 시간은 3분의 1이다. 그래서 쉬움은 두 개까지만 올린다.

체공 시간: 1단계 4.8초 · 2단계 4.2초 · 3단계 3.7초 · 4단계 3.3초 · 5단계 3.0초

## 올라갈 때와 떨어질 때 힘이 다르다

같은 중력으로 떨어뜨리면 올라온 속도 그대로 내려와서 휙 지나간다. 어른이 해도 겨냥할 틈이 없다.
`PHYSICS.fallGravity`에 `FALL_STEPS`의 값(0.13~0.52)을 곱해 **떨어질 때만** 힘을 줄인다.
꼭대기에서 잠깐 떠 있게 되고, 그동안 아이가 그림을 보고 겨냥한다.

대칭 중력이면 2.5초에 그친다.

## 판정은 후하게 둔다

**이 게임에서 제일 중요한 규칙이다.** 여섯 살 손가락은 정확할 수 없다.
분수를 제대로 골랐는데 손이 몇 픽셀 빗나가서 못 벴다면 아이는 자기가 분수를 틀린 줄 안다.
학습을 방해하는 실패는 만들지 않는다.

- `RULES.hitPadding` — 반지름에 여유를 곱해 스친 것도 벤 것으로 친다
- 점이 아니라 **선분**과 원의 거리를 본다. 빠르게 그은 획도 중간이 건너뛰지 않는다
- **놓친 것에는 벌점이 없다.** 콤보만 끊긴다. 화면을 다 훑을 수는 없는데
  놓칠 때마다 하트가 깎이면 금방 끝나 버린다
- 눈에 보이는 오답을 벤 것은 다르다. 그건 운동 실수가 아니라 학습 실수라 하트가 줄어든다

## 좌표계

엔진은 **0~1 좌표**로만 계산한다. x는 화면폭, y는 화면높이를 1로 본다.
픽셀로 바꾸는 일은 `lib/render.ts`가 전부 맡는다.
이렇게 해 두면 세로가 긴 폰과 짧은 폰에서 난이도가 달라지지 않는다.

거리를 잴 때는 y에 `aspect`를 곱해 x축 축척으로 맞춘다 — 안 그러면 세로로 그은 획의
판정이 가로보다 후해진다.

## 확장할 자리

문제 종류는 `types.ts`의 `QuestionMode`에 적혀 있고, 지금은 `find` 하나만 만들었다.

| 모드 | 무엇 |
|---|---|
| `find` | 제시된 분수와 같은 그림을 벤다 (지금) |
| `read` | 그림 하나를 보여주고 맞는 분수 카드를 벤다 |
| `compare` | 제시된 분수보다 큰 것만 벤다 |
| `equivalent` | 서로 크기가 같은 것끼리 벤다 |
| `sum` | 베어 모은 조각의 합이 목표 분수가 되게 한다 |

**새 모드를 넣을 때 손댈 곳은 `lib/problems.ts` 하나뿐이다.**
엔진·렌더러·화면은 그대로 둔다 — 어떤 모드든 결과물이
"문제 한 줄 + 베어야 할 것 목록"으로 같기 때문이다.
정답 판정도 `isCorrectPiece` 한 곳에 모아 두었으니 거기에 `case`만 붙인다.

동치분수는 이미 난이도 안에 녹여 두었다(`allowEquivalent`). 보통 이상에서는
2/4를 1/2로 쳐준다 — 따로 가르치지 않고 몸으로 익히게 하는 자리다.

## 하지 말 것

- 날아다니는 그림에 분수 숫자를 적지 않는다 (위 학습 목표 참고)
- 게임 화면에 `theme-service`를 두르지 않는다. 음식 색이 정답을 가늠하는 단서라
  배경까지 자홍으로 물들이면 수박·케이크 같은 붉은 계열이 묻힌다
- 놓친 것에 벌점을 붙이지 않는다
